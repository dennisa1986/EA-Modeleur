# scripts/

Utility scripts for the MBSE pipeline.  All scripts are standalone and can be run
directly without installing the full package.

| Script | Purpose |
|---|---|
| *(none yet)* | Add scripts here as the project grows |

## Planned scripts
- `export_metamodel.ps1` — EA macro to export the metamodel as XMI 2.1
- `compare_xmi.py` — diff two XMI files at the element level (for golden review)
- `render_pipeline.py` — print a summary of a pipeline run result JSON
